apiVersion: v1
kind: Service
metadata:
  name: todo-svc
  labels:
    app: todo-service
spec:
  type: NodePort
  ports:
  - port: 3000
    nodePort: 30001
    protocol: TCP
  selector:
    app: todo
    zone: prod